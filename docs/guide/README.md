# Vector Nodes — guide

- [Tutorial](tutorial.md) — build, preview, save, and export a network in the editor.
- [Node reference](node-reference.md) — every built-in node (generated from the registry).
- [`.vnodes` format](vnodes-format.md) — the portable JSON network format.
- [Export guide](export.md) — compile a network to a TS/JS module.
- [Performance](performance.md) — how the editor and engine stay fast.

See [`examples/`](../../examples) for runnable `.vnodes` networks (each verified by the
interpreter-vs-compiled conformance suite).
